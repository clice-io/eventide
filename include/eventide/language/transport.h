#pragma once

#include "eventide/jsonrpc/transport.h"

namespace eventide::language {

using Transport = jsonrpc::Transport;
using StreamTransport = jsonrpc::StreamTransport;

}  // namespace eventide::language
